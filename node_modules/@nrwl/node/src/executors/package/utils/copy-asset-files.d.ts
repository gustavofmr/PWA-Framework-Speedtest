import { ExecutorContext } from '@nrwl/devkit';
import { NormalizedBuilderOptions } from './models';
export default function copyAssetFiles(options: NormalizedBuilderOptions, context: ExecutorContext): Promise<{
    success: boolean;
} | {
    error: string;
    success: boolean;
}>;
