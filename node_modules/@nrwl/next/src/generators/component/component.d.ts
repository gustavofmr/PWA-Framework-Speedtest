import type { SupportedStyles } from '@nrwl/react';
import { Tree } from '@nrwl/devkit';
interface Schema {
    name: string;
    project: string;
    style: SupportedStyles;
    directory?: string;
    flat?: boolean;
}
export declare function componentGenerator(host: Tree, options: Schema): Promise<import("../../../../../build/packages/devkit").GeneratorCallback>;
export default componentGenerator;
export declare const componentSchematic: (options: Schema) => (tree: any, context: any) => Promise<any>;
