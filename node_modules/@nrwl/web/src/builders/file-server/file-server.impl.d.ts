import { ExecutorContext } from '@nrwl/devkit';
export interface FileServerOptions {
    host: string;
    port: number;
    ssl: boolean;
    sslKey?: string;
    sslCert?: string;
    proxyUrl?: string;
    buildTarget: string;
    parallel: boolean;
    maxParallel: number;
    withDeps: boolean;
}
export default function fileServerExecutor(opts: FileServerOptions, context: ExecutorContext): AsyncGenerator<{
    success: boolean;
    baseUrl: string;
}, {
    success: boolean;
}, unknown>;
